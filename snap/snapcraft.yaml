name: agent-one
base: core22
version: "0.0.11"
summary: Free AI agent and deep researcher
description: |
  AgentOne is a powerful AI agent that gives you freedom to choose your
  models and control your data.
grade: stable
confinement: strict

layout:
  /usr/lib/$SNAPCRAFT_ARCH_TRIPLET/webkit2gtk-4.1:
    bind: $SNAP/usr/lib/$SNAPCRAFT_ARCH_TRIPLET/webkit2gtk-4.1

apps:
  agent-one:
    command: usr/bin/agent-one
    desktop: usr/share/applications/agent-one.desktop
    extensions: [gnome]
    plugs:
      - network
      - network-bind
      - home
      - audio-playback

package-repositories:
  - type: apt
    components: [main]
    suites: [noble]
    key-id: 78E1918602959B9C59103100F1831DDAFC42E99D
    url: http://ppa.launchpad.net/snappy-dev/snapcraft-daily/ubuntu

parts:
  agent-one:
    plugin: dump
    source: .
    override-build: |
      set -eu
      # The .deb is placed in the source dir by the CI workflow before running snapcraft
      dpkg -x *.deb $SNAPCRAFT_PART_INSTALL/
      # Fix desktop file icon path for snap
      sed -i -e "s|Icon=agent-one|Icon=/usr/share/icons/hicolor/32x32/apps/agent-one.png|g" \
        $SNAPCRAFT_PART_INSTALL/usr/share/applications/agent-one.desktop 2>/dev/null || true
    stage-packages:
      - libwebkit2gtk-4.1-0
      - libayatana-appindicator3-1
